<html>
<head><title>Liberação de acesso</title>
<meta http-equiv=”Content-Type” content=”text/html; charset=utf-8″>
</head>
<body onload="lista()">
    Selecione as tabelas desejadas:<br><br>
    <form>
        <select id="lista" name="lista" multiple>
            
        </select>
    </form>

    <script type="text/javascript">
        
        function lista() {

            var relacao = ['bla','jao'];
            const sb = document.querySelector('#lista');

            for (var i = 0; i < relacao.length; i++) {
                const option = new Option(relacao[i], relacao[i]);
                sb.add(option,undefined);
            }
        }
    </script>
</html>